# Required: SQLite database file path (relative to project root)
DATABASE_URL="file:./dev.db"

# Required: Password to access the internal portal
PORTAL_PASSWORD=your-secret-password-here

# Required for Next.js auth in production (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-nextauth-secret-here
